replicaCount: 1

image:
  repository: ghcr.io/it-top-ai-bot/journal-service
  pullPolicy: Always
  tag: "sha-6f4416d"

ingress:
  enabled: false

config:
  springProfile: "dev,kubernetes"
  redis:
    host: "redis-master.dev.svc.cluster.local"
    port: "6379"
  journal:
    baseUrl: "https://msapi.top-academy.ru/api/v2"
    syncInterval: "3600000"
  configServer:
    host: "config-server-dev.dev.svc.cluster.local"
    port: "8080"
  otel:
    endpoint: "http://192.168.0.160:4318"

resources:
  requests:
    cpu: "250m"
    memory: "512Mi"
  limits:
    memory: "1Gi"
